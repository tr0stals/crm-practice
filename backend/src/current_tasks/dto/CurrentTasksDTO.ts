export class CurrentTasksDTO {
  // deadline: Date;
  // title: string;
  // standId: number;
  // employeeId: number;

  shipmentStandId: number;
  currentTaskStateId: number;
  standTaskId: number;
  executorId?: number;
}
